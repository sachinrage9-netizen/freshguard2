import {useState} from 'react'; import {ChevronDown,ChevronRight,Plus,Pencil,Trash2} from 'lucide-react'; import {childrenOf} from '../data/categoryTree';
export default function CategoryTree({categories,products,onAdd,onEdit,onDelete,parentId=null,depth=0}){const nodes=childrenOf(categories,parentId);return <div className="category-tree">{nodes.map(node=><Node key={node.id} node={node} depth={depth} categories={categories} products={products} onAdd={onAdd} onEdit={onEdit} onDelete={onDelete}/>)}</div>}
function Node({node,depth,categories,products,onAdd,onEdit,onDelete}){const [open,setOpen]=useState(true),kids=childrenOf(categories,node.id),count=products.filter(p=>p.categoryId===node.id).length;return <div className="tree-node" style={{marginLeft:depth*18}}><div className="tree-row"><button className="tree-toggle" onClick={()=>setOpen(!open)}>{kids.length?(open?<ChevronDown size={16}/>:<ChevronRight size={16}/>):'•'}</button><div><strong>{node.name}</strong><small>{count} direct products</small></div><span><button title="Add subcategory" onClick={()=>onAdd(node)}><Plus size={15}/></button><button title="Rename" onClick={()=>onEdit(node)}><Pencil size={15}/></button><button title="Delete" onClick={()=>onDelete(node)}><Trash2 size={15}/></button></span></div>{open&&<CategoryTree categories={categories} products={products} onAdd={onAdd} onEdit={onEdit} onDelete={onDelete} parentId={node.id} depth={depth+1}/>}</div>}
