import {useState} from 'react'; import {useNavigate,Link} from 'react-router-dom'; import {childrenOf,pathFor} from '../data/categoryTree';
export default function AddProduct({setProducts,locations,categories}){const nav=useNavigate();const tops=childrenOf(categories,null);const [form,setForm]=useState({name:'',categoryId:tops[0]?.id||'',quantity:'',expiry:'',locationId:locations[0]?.id||'',batch:'',notes:''});const [notice,setNotice]=useState('');const update=e=>setForm({...form,[e.target.name]:e.target.value});const options=categories.filter(c=>c.id===form.categoryId||pathFor(categories,c.id).map(n=>n).includes(categories.find(x=>x.id===form.categoryId)?.name));function submit(e){e.preventDefault();const location=locations.find(x=>x.id===form.locationId),category=categories.find(x=>x.id===form.categoryId);const product={...form,id:Date.now().toString(),quantity:Number(form.quantity),category:category?.name||'Other',categoryPath:pathFor(categories,form.categoryId),location:location?.name||'Unassigned',priority:'Normal',temp:location?.temperature??null,history:[4,4.1,4.2,4.3,4.2]};setProducts(x=>[...x,product]);setNotice(`${form.name} added to inventory.`);setTimeout(()=>nav('/inventory'),900)}if(!locations.length||!categories.length)return <div className="page form-page"><p className="intro">Create at least one storage location and category before adding products.</p><Link className="primary" to="/settings">Go to Settings</Link></div>;return <div className="page form-page"><p className="intro">Choose the most specific category path for this item.</p>{notice&&<div className="success">✓ {notice}</div>}<form onSubmit={submit}><label>Product name<input required name="name" value={form.name} onChange={update} placeholder="e.g. Mango Ice Cream"/></label><label>Category path<select name="categoryId" value={form.categoryId} onChange={update}>{categories.map(x=><option key={x.id} value={x.id}>{pathFor(categories,x.id).join(' / ')}</option>)}</select></label><p className="path-preview">Selected: {pathFor(categories,form.categoryId).join(' / ')}</p><label>Quantity<input required min="1" type="number" name="quantity" value={form.quantity} onChange={update} placeholder="0"/></label><label>Expiry date<input required type="date" name="expiry" value={form.expiry} onChange={update}/></label><label>Storage location<select name="locationId" value={form.locationId} onChange={update}>{locations.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Batch number (optional)<input name="batch" value={form.batch} onChange={update}/></label><label>Notes (optional)<input name="notes" value={form.notes} onChange={update}/></label><button className="primary" type="submit">Add Product</button></form></div>}
