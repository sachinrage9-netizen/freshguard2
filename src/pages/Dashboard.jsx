import {Link} from 'react-router-dom';
import {StorageCard,ProductCard} from '../components/UI';
import {TriangleAlert,Plus,Package,ArrowRight} from 'lucide-react';
export default function Dashboard({products,sensor,sensorStatus,locations,user,dashboard,alerts=[]}){const type=user.businessType||'Kirana',noun=dashboard.label||({Restaurant:'Ingredients',Dairy:'Dairy products'}[type]||'Products'),urgent=products.filter(p=>p.priority!=='Normal').slice(0,2),show=m=>dashboard.modules.includes(m),warning=alerts.find(a=>a.group==='High priority'&&(a.status||'open')!=='resolved');return <div className={'page dashboard '+type.toLowerCase()}>{show('storage')&&<section><div className="section-heading"><div><p className="eyebrow">{type==='Dairy'?'PRIORITY STORAGE STATUS':'LIVE STORAGE STATUS'}</p><h2>{type==='Restaurant'?'Ingredient storage':'Everything at a glance'}</h2></div><span className="sensor-pill"><i/> {sensorStatus==='error'?'Sensor Unavailable':'Live Sensor'}</span></div><div className="storage-grid">{locations.filter(x=>x.sensorEnabled||x.sensor).map(x=>{const temp=x.temperature==null?'—':`${x.temperature}°C`;const humidity=x.humidity==null?'':` · ${Number(x.humidity).toFixed(1)}% humidity`;return <StorageCard key={x.id} title={x.name} value={temp} detail={`${x.productCount??products.filter(p=>String(p.locationId)===String(x.id)).length} ${noun.toLowerCase()} · ${x.sensor?'Sensor connected':'No recent sensor reading'}${humidity}`}/>})}</div>{sensor.humidity!=null&&<div style={{marginTop:10,fontSize:13,color:'#718078'}}>Live environment: <strong>{Number(sensor.temperature).toFixed(1)}°C</strong> · <strong>{Number(sensor.humidity).toFixed(1)}% humidity</strong></div>}</section>}{show('attention')&&<section><div className="section-heading"><div><p className="eyebrow">NEEDS ATTENTION</p><h2>{type==='Restaurant'?'Use these ingredients first':'Sell these first'}</h2></div><Link className="text-link" to="/inventory">View {noun.toLowerCase()} <ArrowRight size={16}/></Link></div><div className="priority-grid">{urgent.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}{show('warning')&&<section className="warning-banner"><div className="warning-symbol"><TriangleAlert/></div><div><p className="eyebrow">STORAGE CONDITION WARNING</p><h2>{warning?warning.title:'No active storage warning'}</h2><p>{warning?warning.body:'Live storage conditions are currently within the backend-reported state.'}</p><Link to="/alerts">View alerts <ArrowRight size={16}/></Link></div></section>}<section><p className="eyebrow">QUICK ACTIONS</p><div className="quick-actions"><Link to="/add"><Plus/>Add {type==='Restaurant'?'Ingredient':'Product'}</Link><Link to="/inventory"><Package/>View {noun}</Link><Link to="/alerts"><TriangleAlert/>View Alerts</Link></div></section></div>}
