import {useMemo,useState} from 'react'; import {Search,ArrowDownUp} from 'lucide-react'; import {ProductCard} from '../components/UI';
export default function Inventory({products,categories}){const [query,setQuery]=useState('');const [category,setCategory]=useState('All');const [sort,setSort]=useState('expiry');const rows=useMemo(()=>products.filter(p=>(category==='All'||p.categoryId===category)&&p.name.toLowerCase().includes(query.toLowerCase())).sort((a,b)=>sort==='expiry'?a.expiry.localeCompare(b.expiry):(a.priority==='High priority'?-1:a.priority==='Sell soon'&&b.priority==='Normal'?-1:1)),[products,query,category,sort]);return <div className="page"><div className="toolbar"><label className="search"><Search size={19}/><input aria-label="Search products" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search products"/></label><button className="sort" onClick={()=>setSort(sort==='expiry'?'priority':'expiry')}><ArrowDownUp size={17}/> Sort: {sort==='expiry'?'Expiry':'Priority'}</button></div><div className="filters"><button onClick={()=>setCategory('All')} className={category==='All'?'active':''}>All</button>{categories.map(x=><button key={x.id} onClick={()=>setCategory(x.id)} className={category===x.id?'active':''}>{x.name}</button>)}</div><p className="result-count">{rows.length} products</p><div className="inventory-list">{rows.map(p=><ProductCard key={p.id} product={p}/>)}</div></div>}
